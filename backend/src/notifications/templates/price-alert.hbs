<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <style>
    body {
      font-family: Arial, sans-serif;
      line-height: 1.6;
      color: #333;
      max-width: 600px;
      margin: 0 auto;
      padding: 20px;
    }
    .header {
      background-color: #4CAF50;
      color: white;
      padding: 20px;
      text-align: center;
      border-radius: 5px 5px 0 0;
    }
    .content {
      background-color: #f9f9f9;
      padding: 20px;
      border: 1px solid #ddd;
    }
    .price-box {
      background-color: white;
      padding: 15px;
      margin: 15px 0;
      border-left: 4px solid #4CAF50;
    }
    .old-price {
      text-decoration: line-through;
      color: #999;
      font-size: 18px;
    }
    .new-price {
      color: #4CAF50;
      font-size: 24px;
      font-weight: bold;
    }
    .savings {
      background-color: #ffeb3b;
      padding: 10px;
      margin: 10px 0;
      border-radius: 3px;
      text-align: center;
      font-weight: bold;
    }
    .button {
      display: inline-block;
      padding: 12px 24px;
      background-color: #4CAF50;
      color: white;
      text-decoration: none;
      border-radius: 5px;
      margin-top: 15px;
    }
    .footer {
      text-align: center;
      padding: 20px;
      color: #999;
      font-size: 12px;
    }
  </style>
</head>
<body>
  <div class="header">
    <h1>🎉 Price Alert!</h1>
  </div>

  <div class="content">
    <p>Hi {{userName}},</p>

    <p><strong>{{message}}</strong></p>

    <div class="price-box">
      <h3>{{itemName}}</h3>

      {{#if oldPrice}}
      <p>
        <span class="old-price">€{{oldPrice}}</span> →
        <span class="new-price">€{{newPrice}}</span>
      </p>

      <div class="savings">
        💰 You save €{{savings}} (-{{percentageSaving}}%)
      </div>
      {{else}}
      <p class="new-price">€{{newPrice}}</p>
      {{/if}}
    </div>

    {{#if productUrl}}
    <a href="{{productUrl}}" class="button">View Product</a>
    {{/if}}

    <p style="margin-top: 20px; font-size: 14px; color: #666;">
      Don't wait too long - prices can change at any time!
    </p>
  </div>

  <div class="footer">
    <p>JustTrackIt - Your Price Tracking Assistant</p>
    <p>You're receiving this email because you set up a price alert.</p>
  </div>
</body>
</html>
